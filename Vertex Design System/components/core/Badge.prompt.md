Filled micro-label — overlays photography or pins to a card corner ("BUILD #03", "NEW").

```jsx
<Badge tone="gold">Build #03</Badge>
<Badge tone="rare">Last slot</Badge>
```

Use Tag for standalone status in layout; Badge only when it sits *on* something (image, card corner, nav item).
