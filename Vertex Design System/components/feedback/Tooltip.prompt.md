Short annotation on icon-only controls and spec abbreviations.

```jsx
<Tooltip label="Cold start audio"><IconButton name="volume-2" label="Play" /></Tooltip>
```

Inverted (paper on black page), no arrow, no radius. Max ~4 words.
