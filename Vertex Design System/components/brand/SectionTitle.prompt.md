Divides long pages into named sections. The rule beneath it is structural, not decorative.

```jsx
<SectionTitle meta="04 builds">Available builds</SectionTitle>
```

Never use display type for a section title — the luxury/tech display faces are reserved for headlines.
