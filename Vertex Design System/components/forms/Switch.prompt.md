Instant-effect toggle (no save step).

```jsx
<Switch label="Available only" checked={onlyAvailable} onChange={setOnlyAvailable} />
```

Track and knob are both rectangles — a pill switch is off-brand.
