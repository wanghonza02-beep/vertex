Long-form field, used once per form at most.

```jsx
<Textarea label="Aesthetic direction" rows={6}
  placeholder="Paint, leather, stitching, exhaust character…" />
```

Vertical resize only — horizontal resize breaks the grid alignment.
