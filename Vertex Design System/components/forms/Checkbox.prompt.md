Multi-select and consent.

```jsx
<Checkbox label="Add me to the build-slot register" checked={v} onChange={setV} />
```

Never rounded. The tick is drawn in Vertex Black on gold — never a Lucide check glyph.
