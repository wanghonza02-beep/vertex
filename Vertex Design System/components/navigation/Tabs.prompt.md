Section switcher within a page.

```jsx
<Tabs items={['Exterior','Interior','Powertrain']} value={tab} onChange={setTab} />
```

The active indicator is the 1px gold segment of the existing rule — no pill, no background fill.
